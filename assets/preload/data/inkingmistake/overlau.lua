function onUpdatePost(elapsed)

  if curStep >= 35 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 70 then
        doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
        
        end

   if curStep >= 105 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
		if curStep >= 140 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 170 then
        doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
        
        end

   if curStep >= 205 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
		if curStep >= 240 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 310 then
        doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
        
        end

   if curStep >= 345 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
		if curStep >= 380 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 415 then
        doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
        
        end

   if curStep >= 450 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
		if curStep >= 485 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 520 then
        doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
        
        end

   if curStep >= 555 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
		if curStep >= 590 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 625 then
        doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
        
        end

   if curStep >= 660 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
		 if curStep >= 695 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 730 then
        doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
        
        end

   if curStep >= 765 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
		if curStep >= 800 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 835 then
        doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
        
        end

   if curStep >= 870 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
		if curStep >= 905 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 940 then
        doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
        
        end

   if curStep >= 975 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
		if curStep >= 1010 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 1045 then
        doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
        
        end

   if curStep >= 1070 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
	if curStep >= 1105 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 1040 then
        doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
        
        end

   if curStep >= 1075 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
	if curStep >= 1210 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 1245 then
        doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
        
        end

   if curStep >= 1275 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
		 if curStep >= 1310 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 1345 then
        doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
        
        end

   if curStep >= 1380 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
		if curStep >= 1415 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 1450 then
        doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
        
        end

   if curStep >= 1485 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
		if curStep >= 1520 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 1555 then
        doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
        
        end

   if curStep >= 1585 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
		if curStep >= 1625 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 1660 then
        doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
        
        end

   if curStep >= 1695 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
		if curStep >= 1730 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 1765 then
        doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
        
        end

   if curStep >= 1800 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
	if curStep >= 1835 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 1870 then
        doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
        
        end

   if curStep >= 1905 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
		 if curStep >= 1940 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 1975 then
        doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
        
        end

   if curStep >= 2010 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
	if curStep >= 2045 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 2080 then
        doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
        
        end

   if curStep >= 2115 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
		if curStep >= 2150 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 2185 then
        doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
        
        end

   if curStep >= 2220 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
		
		end
		
		if curStep >= 2255 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
  if curStep >= 2285 then
        doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');
        
        end

   if curStep >= 2300 then
		doTweenAlpha('byebye', 'overlay', 0, 0.5, 'currentBeat');
		
		end
		
		if curStep >= 2340 then
		doTweenAlpha('byebye', 'overlay', 1, 0.5, 'currentBeat');

	end
end